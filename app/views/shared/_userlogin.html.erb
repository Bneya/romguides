Esta es la parte del login <br>
<% if user_signed_in? %>
  <%= button_to('Logout', destroy_user_session_path, method: :delete) %>
<% else %>
  <%= link_to('Log-in', new_user_session_path)  %>
  <%= link_to('Sign-in', new_user_registration_path)  %>
<% end %>
