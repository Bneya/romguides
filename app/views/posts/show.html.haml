%h1= @post.title

%p= @post.content

ID de usuario:
%p= @post.user_id

= link_to "Editar", edit_post_path(@post)
= link_to "Borrar post", post_path(@post), method: :delete, data: {confirm: "Estás seguro de borrar?"}
= link_to "Volver a casa", root_path
